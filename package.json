{
  "name": "tripcost",
  "version": "1.0.0",
  "description": "web application to log a list of trips, and to track expenses for those trips",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Jordan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.6.6"
  }
}
